export const environment = {
  production: false,
  /**
   * With `ng serve`, this uses the proxy in proxy.conf.json → http://localhost:5214
   * (matches SmartHire backend launchSettings "http" / "https" profile).
   *
   * If you open the built app without the dev server, set the full URL to match your console output, e.g.:
   * 'https://localhost:7220/api' or 'http://localhost:5214/api'
   */
  apiUrl: '/api'
};
